// Viết chương trình JavaScript:
// - Nhập vào một chuỗi kí tự
// - Trích xuất ra chuỗi con từ nó
// - In ra màn hình console hoặc alert nội dung của chuỗi con

// Gợi Ý: dùng hàm substring(min_index, max_index);
var str = prompt("Nhập chuỗi ");

var trich_xuat = str.substring(2, 4);

console.log("trích xuất là : " + trich_xuat);
