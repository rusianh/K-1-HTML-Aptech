/**Bài Tập HTML-7.4-String toLowerCase
 * Viết chương trình JavaScript:
- Nhập vào một chuỗi kí tự.
- Chuyển đổi thành in thường.
- In ra màn hình console hoặc alert nội dung của chuỗi mới.

Gợi Ý: dùng hàm toLowerCase();
 */

var insert_string = prompt("nhap chuoi");
var string_lowcase = insert_string.toLowerCase();
console.log("Chuỗi in hoa là: " + string_lowcase);

